# 服務所有 hosts 所有 paths
:80

# 禁用 HTTPS, reverse proxy server 已為我們負責傳輸安全的部分...
tls off

# 根目錄
root /var/www

# 預設副檔名
ext .html .md

markdown {
  # 路徑相對於網站的根目錄, i.e. /var/www
  template blog blog.template.html
}